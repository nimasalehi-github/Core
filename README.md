# Core

Core/
├── Package.swift
└── Sources/
    └── Core/
        ├── Core.swift          ← فایل پایه (ساختار و ورژن)
        ├── Logger.swift        ← مدیریت لاگ‌ها و print-trace
        └── ErrorHandler.swift  ← کنترل و گزارش خطاها
        
        
        🧠 هدف فایل Core.swift

این فایل حکم درگاه ورودی پکیج رو داره.
در آینده هر پروژه‌ای که از این پکیج استفاده کنه، فقط با import Core به تمام اجزا دسترسی خواهد داشت. 

🧠 Snapshot اجرای Core
مرحله    رویداد    توضیح فارسی    خروجی کنسول
1    let core = Core()    نمونه ساخته می‌شود    [ℹ️ INFO] Core.swift:13 init() — Core initialized ✅
2    core.performTestOperation()    شروع عملیات تست    [ℹ️ INFO] Core.swift:19 performTestOperation — Performing test operation in Core.
3    (نتیجه موفق)    عملیات موفق    [ℹ️ INFO] Core.swift:24 performTestOperation — Operation succeeded 🎉
4    (نتیجه شکست)    خطای شبیه‌سازی‌شده    [❌ ERROR] Core.swift:27 performTestOperation — Error: Simulated failure


🧱 فایل ۱: Logger.swift



این فایل مسئول ثبت و نمایش رویدادها، وضعیت‌ها و خطاهاست.
یعنی هر ماژولی در آینده (Networking, ViewModel, Repository, …) می‌تونه با Logger.shared.log(...) پیام‌هاشو ردگیری کنه.


🧱 فایل ۲: ErrorHandler.swift

این فایل وظیفه دارد خطاهای عمومی را در سراسر اپ مدیریت کند (چه در لایهٔ شبکه، چه UI).
در آینده با Logger ترکیب می‌شود تا گزارش‌های کامل تولید کند.
🌐 NetworkManager — با پشتیبانی از async/await

این ماژول ستون فقرات همهٔ اپ‌های آیندهٔ ما خواهد بود.

🎯 هدف:

ما می‌خواهیم یک کلاس ساده اما قابل‌گسترش بنویسیم که:

درخواست‌های HTTP را با async/await اجرا کند.

پاسخ را به مدل دلخواه (مثل Decodable) تبدیل کند.

تمام خطاها را از طریق ErrorHandler و Logger گزارش کند.

در آینده بتواند برای APIهای مختلف استفاده شود.
🔹 Breakpoints پیشنهادی

روی خطوط /11/, /14/, /17/ برای بررسی دادهٔ دریافتی، response، و خطاها.

در Console، خروجی Logger رو ببین تا جریان کامل درخواست را دنبال کنی.
